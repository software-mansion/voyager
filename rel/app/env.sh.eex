export RELEASE_MODE="interactive"
export RELEASE_DISTRIBUTION=none
# Never use the build-time releases/COOKIE — it is identical across all installs (VOY-219).
export RELEASE_COOKIE="$(od -An -tx1 -N32 /dev/urandom | tr -d ' \n')"

cd "$HOME"
